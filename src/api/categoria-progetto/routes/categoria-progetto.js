'use strict';

/**
 * categoria-progetto router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::categoria-progetto.categoria-progetto');
