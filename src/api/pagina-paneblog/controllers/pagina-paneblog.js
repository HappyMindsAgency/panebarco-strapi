'use strict';

/**
 * pagina-paneblog controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::pagina-paneblog.pagina-paneblog');
