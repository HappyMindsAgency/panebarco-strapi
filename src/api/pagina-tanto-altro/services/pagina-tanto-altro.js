'use strict';

/**
 * pagina-tanto-altro service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::pagina-tanto-altro.pagina-tanto-altro');
