'use strict';

/**
 * pagina-cookie-policy controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::pagina-cookie-policy.pagina-cookie-policy');
