'use strict';

/**
 * pagina-cookie-policy router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::pagina-cookie-policy.pagina-cookie-policy');
